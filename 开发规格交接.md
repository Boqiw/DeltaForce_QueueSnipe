# 三角洲行动主播撞车监测系统 — 开发规格交接文档

> 文档用途：本文件是交付给编程 agent 的**唯一开发依据**。所有产品决策、技术选型、边界与红线均已收敛（经 8 轮讨论与市场/法律/技术调研），编程 agent 按此实现，**不要重新设计、不要扩展范围、不要擅自增加功能**。
>
> 版本：v1.0 ｜ 日期：2026-09-01

---

## 0. 项目一句话

跨平台实时监测游戏主播直播中的「撞车」事件（两名主播在同一局游戏中），通过 **CV 识别直播画面左上角的对局代码**（非爬虫、非 API）判定同局，自动录制素材，经主播授权后自动剪辑出片发布变现。

**定位**：个人自部署工具集 + 运营工作流。非 SaaS、非创业项目、无外部投资。开发者本人即唯一用户与运营者。

---

## 1. 项目背景与决策依据（编程 agent 必读）

以下事实决定了系统设计，**不要试图用"更简单"的方案绕过**：

| 背景事实 | 对设计的影响 |
|---|---|
| 游戏官方 / 第三方查询 API **只允许查自己**，无"按对局查玩家列表"公开接口 | 撞车判定**只能靠画面层**：识别直播画面中的对局代码，做代码匹配 |
| 腾讯官方"游戏来联"已有平台级撞车能力，但**仅限腾讯系平台**（斗鱼/虎牙/快手/B站），**不覆盖抖音**（字节-腾讯商业对峙） | 抖音是唯一空白平台，但用户同样拿不到腾讯数据 → 必须靠画面识别，这就是本系统的立身之本 |
| 直播画面（主播形象/声音/解说）版权**归主播个人**；游戏素材官方提供 Tier3 二创授权 | 内容链合规前提：**主播画面必须获得授权才能剪辑发布**；游戏侧素材走官方 Tier3 |
| 抖音对盗播/搬运打击极严（已处置 10 万+直播间） | 系统**严禁**未经授权自动发布任何主播画面 |
| 家用电脑资源有限（约 100Mbps 下行 + 持续写盘为上限） | 并发录制路数受限，MVP 5-10 路；默认整局录制，带宽/磁盘吃紧时降级轮询录制 |

---

## 2. 系统架构（五层）

```
┌─────────────────────────────────────────────────────────┐
│ L5 内容层  ffmpeg 自动切段 + 双视角拼接 + whisper 字幕    │  ← 变现端
│ L4 服务层  (远期) 对外查询 + 小程序，MVP 阶段不做         │
├─────────────────────────────────────────────────────────┤
│ L3 事件层  SQLite 落库 + 撞车判定 + 运营面板实时展示      │
│ L2 识别层  OpenCV ROI 裁剪 + RapidOCR + 正则 + 多帧确认   │  ← 核心差异化
│ L1 采集层  ffmpeg 拉流/分片 + 平台解析库                  │  ← 唯一灰区风险
└─────────────────────────────────────────────────────────┘
```

- **L1 采集层**：拉取直播流并录制。虎牙/斗鱼/B站 用 `streamlink`（稳定）；抖音**无官方接口**，用社区逆向解析库（GitHub 搜 douyin live 解析，依赖 X-Bogus 签名算法，**生命周期短、随时失效**——架构必须做到"解析源可替换"，失效时换库即可，不与它较劲）。
- **L2 识别层**：从直播帧左上角固定 ROI 识别对局代码。OpenCV 裁图 → RapidOCR 识别 → 正则约束格式 → **连续 N 帧一致才确认**。
- **L3 事件层**：对局代码相同 + 时间窗口重叠 = 撞车事件。SQLite 落库，在**本地运营面板**实时展示（提示运营者"快去私信主播"）。
- **运营面板**（MVP 必需，属 L3）：本地 Web 页面（仅 localhost 访问，Flask + 原生 HTML/JS），展示监控主播在线状态、撞车事件列表、素材清单与授权状态。**只给运营者本人看，不做任何外部推送**。
- **L4 服务层**：**MVP 明确不做**。远期对外查询服务 + 小程序（"查询战绩"类目个人主体过审极难，属行政门槛）。注意与运营面板区分：面板是本地单机工具，L4 是对外服务。
- **L5 内容层**：撞车事件素材 → ffmpeg 自动切段、分屏拼接、whisper 加字幕 → 人工过目发布。

---

## 3. 功能清单（按里程碑拆分）

### 3.1 MVP（技术关验证，目标 1 个周末，5-10 路）
| ID | 功能 | 说明 |
|---|---|---|
| F1 | 直播拉流 | 虎牙/斗鱼/B站 via streamlink；抖音 via 逆向解析库（可替换） |
| F2 | 对局代码识别 | 固定 ROI + RapidOCR + 正则 + 连续 N 帧确认 |
| F3 | 撞车判定 | 两路会话对局代码相同 + 时间窗口重叠 → 撞车事件 |
| F4 | 事件落库与界面展示 | SQLite 记录 + 本地运营面板（localhost Web 页面）实时显示撞车事件：双方主播、对局代码、命中时刻、素材状态；**不做任何外部推送** |
| F5 | 整局录制 | 从识别到对局代码（进入游戏）起录制整局画面，至该局结束（代码消失/换局/下播）停止；撞车判定后双方各自持有完整局内素材 |
| F6 | 单主播闭环验证 | 至少 1 路完整跑通：拉流→识别→落库→面板显示→整局录制 |

### 3.2 V1（技术关通过 + 商务关验证后）
| ID | 功能 | 说明 |
|---|---|---|
| F7 | 自动出片流水线 | 从双方整局素材中切出撞车时段（t-3min ~ t+8min）→ ffmpeg 切段 → 左右分屏/画中画 → whisper 字幕 → 模板片头，10 分钟出片 |
| F8 | 授权状态机 | 素材状态：`待授权 → 已授权 → 可发布` / `未授权 24h 后自动删除` |
| F9 | 主播库管理 | 头部 20-50 名主播的增删改查、启停监控、优先级排序 |

### 3.3 V2（需求被真实验证后才做，MVP 阶段禁止投入）
| ID | 功能 | 说明 |
|---|---|---|
| F10 | 查询服务 | FastAPI 后端，玩家查"我与主播是否撞车"（需先有足够主播库） |
| F11 | 小程序 | 个人主体"查询战绩"类目过审难，最后做，仅作引流 |

---

## 4. 技术栈

| 层 | 技术 | 备注 |
|---|---|---|
| 语言 | Python 3.12+ | 全栈单语言 |
| 采集 | ffmpeg（核心）+ streamlink + 抖音社区解析库 | ffmpeg 是唯一不可替代的技能点 |
| 识别 | OpenCV + RapidOCR（PaddleOCR 轻量版） | 对字母数字识别绰绰有余 |
| 存储 | SQLite | 单机足够 |
| 推送 | 无（改为本地运营面板展示） | MVP 不做任何外部推送 |
| 面板 | Flask + 原生 HTML/JS（localhost） | 本地运营界面，仅本机访问 |
| 出片 | ffmpeg + faster-whisper | 字幕可选 |
| 服务层（远期） | FastAPI + 小程序 | MVP 不做 |
| 运行环境 | Windows 本地 PC | 用户环境 |

---

## 5. 数据模型（SQLite 表设计草案）

```sql
-- 主播表
streamers (
  id INTEGER PRIMARY KEY,
  platform TEXT NOT NULL,        -- 'douyin' | 'huya' | 'douyu' | 'bilibili'
  room_id TEXT NOT NULL,         -- 直播间号
  name TEXT NOT NULL,
  monitor_enabled INTEGER DEFAULT 1,
  priority INTEGER DEFAULT 5,    -- 1-10，头部主播优先
  UNIQUE(platform, room_id)
);

-- 直播会话（一次开播）
live_sessions (
  id INTEGER PRIMARY KEY,
  streamer_id INTEGER NOT NULL REFERENCES streamers(id),
  started_at TEXT NOT NULL,
  ended_at TEXT,
  stream_url TEXT                -- 解析到的拉流地址（失效即重解析）
);

-- 对局代码观测记录
match_code_observations (
  id INTEGER PRIMARY KEY,
  session_id INTEGER NOT NULL REFERENCES live_sessions(id),
  match_code TEXT NOT NULL,      -- 画面识别出的对局代码
  first_seen_at TEXT NOT NULL,
  last_seen_at TEXT NOT NULL,
  confirm_frames INTEGER DEFAULT 0,   -- 连续确认帧数
  status TEXT DEFAULT 'active'   -- active | ended
);

-- 撞车事件
collision_events (
  id INTEGER PRIMARY KEY,
  match_code TEXT NOT NULL,
  started_at TEXT NOT NULL,
  ended_at TEXT,
  status TEXT DEFAULT 'open'     -- open | closed，面板按状态展示
);

-- 撞车事件 ↔ 会话 关联
collision_sessions (
  collision_id INTEGER NOT NULL REFERENCES collision_events(id),
  session_id INTEGER NOT NULL REFERENCES live_sessions(id),
  PRIMARY KEY (collision_id, session_id)
);

-- 录制素材
recordings (
  id INTEGER PRIMARY KEY,
  collision_id INTEGER NOT NULL REFERENCES collision_events(id),
  streamer_id INTEGER NOT NULL REFERENCES streamers(id),
  file_path TEXT NOT NULL,
  start_offset_sec INTEGER NOT NULL,  -- 相对撞车时刻
  end_offset_sec INTEGER NOT NULL,
  status TEXT DEFAULT 'pending_auth', -- pending_auth | authorized | rejected | deleted
  created_at TEXT NOT NULL
);

-- 授权记录
authorizations (
  id INTEGER PRIMARY KEY,
  recording_id INTEGER NOT NULL REFERENCES recordings(id),
  channel TEXT NOT NULL,             -- 私信 | 微信 | 其他
  evidence TEXT,                     -- 授权凭证（聊天记录截图路径/文字）
  authorized_at TEXT NOT NULL
);

-- 产出视频
videos (
  id INTEGER PRIMARY KEY,
  collision_id INTEGER NOT NULL REFERENCES collision_events(id),
  output_path TEXT NOT NULL,
  published INTEGER DEFAULT 0,
  published_at TEXT,
  views INTEGER DEFAULT 0
);
```

> 表结构可微调，但**状态机语义**（尤其 recordings.status）必须保留。

---

## 6. 核心逻辑细节

### 6.1 对局代码识别（L2）
1. **ROI 定位**：直播画面左上角固定区域（具体坐标需在真实画面上标定一次，做成配置文件）。
2. **采样频率**：每 2 秒取 1 帧（可配置）。
3. **识别链路**：OpenCV 裁剪 ROI → RapidOCR → 正则过滤（字母数字、长度范围——**对局代码确切格式待真实画面实测后收紧**，先用宽松正则 + 多帧确认兜底）。
4. **确认机制**：同一代码**连续 3-5 帧一致**才写入 `match_code_observations`，防弹幕/UI 闪烁误判。
5. **兜底**：单帧识别失败不阻塞，下一帧重试；连续 30 帧无有效代码则标记该会话"代码不可见"并告警（可能是直播画面遮挡/主播未进对局）。

### 6.2 撞车判定（L3）
- 两条 `live_sessions` 的当前 `match_code` 相同，且**时间窗口重叠**（如同一代码的首见时间差 < 5 分钟）→ 生成 `collision_events`。
- 边界处理：
  - 主播中途换局 → 代码变化时旧观测置 `ended`，新观测开启。
  - 主播处于观战/结算画面 → 可能短暂无代码或代码残留，靠多帧确认 + 时间窗过滤。
- 命中即在**运营面板**实时显示（顶部高亮、倒序排列），内容为："[A主播] 与 [B主播] 撞车（代码 XXX，t 时刻）"，运营者看到后去私信。

### 6.3 录制策略（L1）
- **整局录制**：从**识别到对局代码**（进入游戏）即开始录制，到**该局结束**（代码消失/变化/主播下播）停止。识别确认有 3-5 帧延迟（约 6-10 秒），录制启动时需**向前回退 30 秒**（保留进入游戏的画面），避免漏掉局头。
- **触发模型**：录制跟随 `match_code_observations.status` 生命周期——`active` 期间持续录制，置 `ended` 时停止并落盘为**一整局素材文件**；撞车判定只是**消费**双方已有的整局素材，不触发额外录制。
- **磁盘估算**：720p/约 1Mbps 时一局（约 25 分钟）≈ 200MB；10 路 × 每晚 5 局 ≈ 10GB/晚。需规划磁盘空间与素材保留策略（授权后转码压缩；未授权 24h 自动删除，见第 7 节）。
- **并发上限**：MVP 5-10 路；跑通后扩展至头部 20-30 路；若带宽/磁盘吃紧，可降级为**轮询录制**（每路仅在对局代码可见时段录制，其余时段释放资源）——但整局录制是默认策略。
- **落盘格式**：`{platform}_{room_id}_{match_code}_{start_ts}.ts`，后续 ffmpeg 从整局素材中按时间戳切段。

### 6.4 自动出片流水线（L5，V1 实现）
1. 从双方整局素材中按时间戳切出撞车时段（双方各一段，`t-3min ~ t+8min`）。
2. 左右分屏 / 画中画拼接双视角（ffmpeg 一行命令）。
3. whisper 生成字幕（可选，烧录或外挂）。
4. 模板片头 + 结尾二维码/引导关注（可配置图片）。
5. 产出 `videos` 记录，人工过目 → 发布。

---

## 7. 运营工作流（授权流程，系统必须支撑）

```
撞车命中 → 运营面板显示 → 运营者查看事件并私信双方主播
   ├─ 主播同意授权 → 素材标记 authorized → 自动出片 → 发布
   └─ 主播拒绝/无回复 → 24 小时后素材自动删除（pending_auth 超时任务）
```

- 运营面板提供**素材清单**：列出所有 `pending_auth` 素材、撞车双方、时间戳、状态，运营者逐条私信。
- 授权凭证（聊天记录）归档到 `authorizations` 表，留存证据。
- **铁律：未授权素材绝不发布，24h 内必须可自动删除。**

---

## 8. 验收标准

### 技术关（MVP）验收
1. 5-10 路真实直播流连续跑 1 小时，0 崩溃。
2. 对局代码识别率 ≥ 95%（抽样人工核对 100 帧，定义：帧级命中率）。
3. 撞车事件从识别到运营面板显示延迟 < 1 分钟（含 3-5 帧确认时间）。
4. 单机 CPU/内存/带宽占用在可接受范围（CPU < 80%，带宽 < 100Mbps）。
5. 整局录制起止正确：识别到代码即开始、该局结束即停止（含 30 秒回退）；撞车时双方整局素材完整可查。
6. 抖音解析源失效时，系统可**通过配置切换到新库**，不修改业务代码。

### 商务关 / 产品关（非编程验收，供参考）
- 商务关：私信 10 名主播，≥ 2 名授权。
- 产品关：C 端查询需求被真实付费/高频使用验证后，才做 V2。

---

## 9. 红线与硬约束（编程 agent 必须遵守）

1. **法律红线**：系统不得在无授权状态下自动发布任何主播画面内容；`pending_auth` 素材 24h 自动删除是硬性功能，不可省略。
2. **数据获取边界**：只用**画面识别**，不写爬虫、不碰非官方 API 接口（战绩查询类第三方接口同样禁用）。
3. **不做的事**（明确非目标）：
   - 不做实时直播转播/盗播功能；
   - 不做"故意撞车"相关工具（官方打击对象）；
   - 不做小程序（MVP 阶段）；
   - 不做多用户/多租户，单用户单机。
4. **可维护性**：模块解耦（采集/识别/事件/存储/面板/输出独立），配置全部外置（主播列表、ROI、采样率、轮询策略、运营面板端口、抖音解析库开关），结构化日志便于诊断拉流失败。

---

## 10. 开发顺序建议

1. **单路闭环**（半天）：1 路真实直播流 → 识别 → 落库 → 运营面板显示 → 整局录制。
2. **多路扩展**（半天）：并发管理器 + 每路独立进程/协程 + 资源上限控制。
3. **撞车判定与面板展示**（半天）：事件模型 + 时间窗逻辑 + 面板事件列表实时刷新。
4. **整局录制落盘**（半天）：跟随对局生命周期起止录制 + 30 秒回退 + 磁盘清理策略。
5. **配置与日志打磨**（收尾）：让所有参数可配置，跑通 1 小时稳定性测试。

> 优先保证**稳定性与可观测性**（日志、告警），识别准确率可通过调参迭代，拉流失败必须能被日志清楚地暴露出来。

---

## 11. 项目状态与优先级（给项目所有者的提醒，非编程内容）

- 项目定位：**待验证的实验项目**，非主线。所有者的主线是 RA 申请/留学（2026-09 起）。
- 技术关周末实验应排在主线事务之后。
- 本系统全部内容基于 2026-08-29 八轮讨论收敛结论，决策记录见工作区 `.workbuddy/memory/2026-08-29.md`。
